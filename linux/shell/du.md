# du


```bash
$ ls -lF # -F 文件夹后加/,可执行文件后面加*号
$ ls -lSh ./  # 文件按从大到小显示
$ ls -lShr ./  # 文件按从小到大显示
$ ls -lth ./ # 文件按最近修改时间排序

$ du -sh . # 查看当前目录大小
$ du -sh * | sort -hr # 显示当前文件夹下文件大小
$ du -ah | sort -hr | head -n10 # 显示当前文件夹下文件大小排名前十的文件
```